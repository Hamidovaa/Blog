@model List<ArticleViewModel>

<div class="container-xxl flex-grow-1 container-p-y">
    <h1>Pending Approval Articles</h1>
    <a class="btn btn-success mb-3" asp-controller="Article" asp-action="Add">
        Create Article <i class="bx bx-plus"></i>
    </a>
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Category</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
         <tbody>
            @foreach (var article in Model)
            {
                <tr>
                    <td>@article.Title</td>
                    <td>@article.Category.Name</td>
                    <td>
                        @if (article.Status == Blog.Entity.Entities.ArticleStatus.PendingApproval)
                        {
                            <span class="badge bg-warning" style="width:120px; padding:10px;">Pending Approval</span>
                        }
                        else if (article.Status == Blog.Entity.Entities.ArticleStatus.Approved)
                        {
                            <span class="badge bg-success" style="width:120px; padding:10px;">Approved</span>
                        }
                        else if (article.Status == Blog.Entity.Entities.ArticleStatus.Rejected)
                        {
                            <span class="badge bg-danger" style="width:120px; padding:10px;">Rejected</span>
                        }
                    </td>
                   
                    <td>
                        <a class="btn btn-primary btn-sm" data-id="@article.Id"  asp-controller="Article" asp-action="Update" asp-route-articleId="@article.Id"><i class="bx bxs-edit"></i></a>
                 
                        <a class="badge bg-danger" data-id="@article.Id"  asp-controller="Article" asp-action="Delete" asp-route-articleId="@article.Id"><i class="bx bx-minus-circle"></i></a>

                    </td>

                   
                   
                </tr>
            }
        </tbody>
    </table>
</div>
